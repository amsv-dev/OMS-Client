---
- name: Configure monitoring hooks
  hosts: all
  gather_facts: false
  tasks:
    - name: Simulate monitoring configuration
      ansible.builtin.debug:
        msg: "configure_monitoring executed (global pack)"
