---
- name: Update package metadata
  hosts: all
  gather_facts: false
  tasks:
    - name: Simulate package update
      ansible.builtin.debug:
        msg: "update_packages executed (no-op by default)"
